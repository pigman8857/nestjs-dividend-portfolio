# Work Log — 2026-03-15

**Project:** MongoOracle
**Focus:** OCI ADB provisioning debugging + integration verification

---

## Timeline

| Time (UTC+7) | Activity |
| ------------ | -------- |
| 13:29–14:49  | Terraform infra debugging (round 1) — identified and fixed 6 provisioning issues |
| 14:59        | End-to-end API test run against live OCI ADB — 11/11 PASS |
| 15:29        | Test result and work log documented |
| 16:59–17:07  | Terraform infra debugging (round 2) — full destroy + apply cycle; fixed 3 provisioner issues |
| 17:36        | Second-round API test + performance baseline — all tests PASS |

---

## Round 1 — Issues Found and Fixed (13:29–14:49)

Six Terraform infra issues were identified and resolved. Full detail — root causes, symptoms, and fixes — documented in the infra repo:

**[nestjs-dividend-portfolio-infra — issue-fix-summary-01.md](https://github.com/pigman8857/nestjs-dividend-portfolio-infra/blob/main/docs/issue-fix-summaries/issue-fix-summary-01.md)**

Summary of issues covered:

1. Wrong ADB hostname domain
2. DB user never created (silent provisioner failure)
3. ORDS schema not enabled
4. Invalid `GRANT CREATE INDEX` (ORA-00990)
5. Wrong MongoDB database name in URI
6. `compute_count` drift on Always Free ADB

### Round 1 Verification

After all six fixes were applied, a full end-to-end API test was run at **14:59 UTC+7**.

All 7 application domains were exercised: Users, Assets, Portfolios, Trades (ACID transactions), Price Ticks (Time Series), Dividends (Time Series), and Alerts (event-driven trigger).

**Result: 11/11 tests PASS**

Full test detail: [`docs/test-result/oci_adb_result_20260315_145919.md`](../test-result/oci_adb_result_20260315_145919.md)

---

## Round 2 — Issues Found and Fixed (16:59–17:07)

A full `terraform destroy` + `apply` cycle was run to validate that the automated provisioner works end-to-end without any manual SQL intervention (no OCI Console → Database Actions → SQL Worksheet required). Three further provisioner issues were found and fixed.

**[nestjs-dividend-portfolio-infra — issue-fix-summary-02.md](https://github.com/pigman8857/nestjs-dividend-portfolio-infra/blob/main/docs/issue-fix-summaries/issue-fix-summary-02.md)**

Summary of issues covered:

1. `ORDS.ENABLE_SCHEMA` silently not executed — missing `/` PL/SQL block terminator
2. SQL failures invisible to Terraform — ORDS always returns HTTP 200; response body was discarded
3. Retry loop could fall through after exhausting ORDS readiness retries (latent defect, fixed proactively)

### Round 2 Verification

After all three fixes, `terraform apply` fully provisioned ADB, created the DB user, and enabled the ORDS schema automatically with no manual steps.

A second full end-to-end API test was run at **17:36 UTC+7**, expanded to include a performance baseline.

**Result: 11/11 tests PASS**

Full test detail (with performance baseline): [`docs/test-result/oci_adb_result_20260315_173637.md`](../test-result/oci_adb_result_20260315_173637.md)

---

## Key Learnings

- OCI ADB MongoDB hostnames must be read from resource outputs — never constructed manually.
- Terraform `null_resource` provisioners that depend on external tools should always validate tool presence or be replaced with HTTP-based alternatives (ORDS REST SQL works well here).
- ORDS schema enablement is a mandatory step for MongoDB wire protocol access — it is separate from user creation.
- Oracle does not support `GRANT CREATE INDEX`; index creation on owned tables is implicit.
- MongoDB URI database name must equal the Oracle schema/username on ADB.
- Always Free ADB instances require `lifecycle { ignore_changes = [compute_count] }` to avoid 403 errors on re-apply.
- PL/SQL anonymous blocks sent to the ORDS REST API require a `/` terminator — without it, ORDS buffers the block silently and executes nothing, returning HTTP 200.
- The ORDS REST API (`/ords/admin/_/sql`) always returns HTTP 200 regardless of SQL success or failure — errors are in the response body and must be explicitly checked.
