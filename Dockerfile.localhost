FROM node:22-alpine

WORKDIR /app

COPY package*.json ./
RUN npm ci

COPY tsconfig*.json nest-cli.json ./
# src/ is mounted as a volume at runtime for hot reload

EXPOSE 3000
CMD ["npm", "run", "start:dev"]
